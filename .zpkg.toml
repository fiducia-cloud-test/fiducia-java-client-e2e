# generated by zed-pkg-test/test-org-fleet; edit the fleet manifest or bootstrapper, not this file.
[package]
org = "fiducia-cloud-test"
name = "fiducia-java-client-e2e"
version = "0.1.0"
description = "java full API parity; locks semaphores idempotency rate limits cron KV election discovery; redirect refusal; retry safety; artifact packaging"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/fiducia-cloud-test/fiducia-java-client-e2e"

[install]
dir = ".vendor/.zed"

[dependencies]
"fiducia-cloud/fiducia-clients" = "^0.1.0"
